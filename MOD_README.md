# PixelLakeHeart — Mod 开发手册 & 局域网联机指南

游戏支持 **Mod 拓展** 和 **局域网联机（同湖共钓 + 共享排行榜）**。

---

## 1. 如何放入 Mod

把 `.mod` 文件放进游戏可执行文件（`.exe`）旁边的 **`mods/`** 文件夹即可，游戏启动时会自动扫描加载该目录下所有 `.mod` 文件。

```
PixelLakeHeart.exe
mods/
  ├─ easy_mode.mod
  ├─ gold_carp.mod
  ├─ heart_sprite.mod
  └─ rich_fish.mod
```

> 修改 mod 后需要 **重启游戏** 才能生效。所有 `.mod` 文件同时生效，同名指令以后加载的为准。

---

## 2. Mod 文件语法

Mod 是纯文本（UTF-8，建议用记事本/VS Code 编辑），每行一条指令。以 `#` 开头的是注释，会被忽略。

支持 5 种指令：`fish` / `mult` / `shop` / `palette` / `sprite`。

### 2.1 `fish` — 修改某条鱼的属性

```
fish <编号> value=.. exp=.. weight=.. diff=..
```

| 参数 | 含义 | 取值范围 |
|------|------|----------|
| `value` | 出售价格 | 0～100000 |
| `exp`   | 获得的经验 | 0～10000 |
| `weight`| 重量（影响体型/判定） | 1～10000 |
| `diff`  | 上钩难度 | 0～4（越大越难） |

示例 —— 让 6 号鱼（金鱼）很值钱：
```
fish 6 value=9999 exp=500 weight=80 diff=1
```

### 2.2 `mult` — 全局倍率（所有鱼生效）

```
mult coins=.. xp=..
```

| 参数 | 含义 | 取值范围 |
|------|------|----------|
| `coins` | 金币倍率（例如 `2` 表示钓到钱翻倍） | 1～1000 |
| `xp`    | 经验倍率 | 1～1000 |

示例 —— 双倍金币、五倍经验：
```
mult coins=2 xp=5
```

### 2.3 `shop` — 修改商店售价

```
shop <ROD|LURE|NET|BOAT> cost=..
```

物品名（必须大写）：`ROD` 鱼竿、`LURE` 鱼饵、`NET` 渔网、`BOAT` 小船。

示例 —— 鱼竿降价：
```
shop ROD cost=10
```

### 2.4 `palette` — 修改某条鱼的某个调色板颜色

每条鱼的像素图板子里有 16 个颜色索引（`0`～`15`）。`sprite` 指令里的字符 `0`~`9` / `a`~`f` 就对应这些索引。

```
palette <编号> <颜色索引0-15> r=.. g=.. b=..
```

示例 —— 把 3 号鱼的调色板索引 4 改成金色 `(255,215,0)`：
```
palette 3 4 r=255 g=215 b=0
```

### 2.5 `sprite` — 整体替换某条鱼的 64×64 像素图

先写一行 `sprite <编号>`，**下一行起连续写 64 行、每行 64 个字符** 作为像素图：

- 字符 `0`~`9` / `a`~`f`（大小写均可）→ 使用对应调色板索引的颜色；
- 空格 `.` 或其它字符 → 透明。

参考示例 `mods_examples/heart_sprite.mod`（把鲤鱼替换成一颗红心）。

---

## 3. 游戏内置的 Mod 示例

### easy_mode.mod
```
mult coins=2 xp=2
shop ROD cost=15
shop LURE cost=20
shop NET cost=25
shop BOAT cost=75
```
开局更轻松：金币/经验双倍，商店全面打折。

### rich_fish.mod
把全部鱼的售价和经验大幅提高：
```
fish 0 value=500 exp=50
fish 1 value=600 exp=60
...（对每条鱼逐一设置）
```

### gold_carp.mod
把鲤鱼调成金色：
```
palette 0 4 r=255 g=215 b=0
palette 0 5 r=255 g=180 b=0
```

### heart_sprite.mod
用 `sprite 0` 把鲤鱼整张像素图替换成一颗爱心。

---

## 4. 局域网联机（同湖共钓 + 共享排行榜）

基于 **UDP 无头同步**，无需外网、无需账号，同一局域网内即可同湖钓鱼，并共享排行榜（比钓鱼总价值/条数）。

### 4.1 操作方式
1. 在有字标题界面按方向键进入 **局域网联机** 菜单（`SPACE` 确认）。
2. **创建房间**：主机选「创建房间」，自动开房等待其他人加入。
3. **加入房间**：其它玩家选「加入房间」，输入主机的 IP 地址（`数字` 和 `.`，确认键 `SPACE`/`ENTER` 加入，`ESC` 返回）。
4. 进入同一片湖后，你能看到其他玩家的实时位置，钓鱼结果会写入共享排行榜。

### 4.2 说明
- 默认端口：`25000`（可在 `MOD_README` 所指的源码中 `NET_PORT` 常量修改）。
- 主机需处于可达状态（同一局域网，防火墙放行该 UDP 端口）。
- 建议先由主机「创建房间」，再让其它玩家「加入」。

---

## 5. 常见问题

- **Mod 没生效？** 确认文件是 `.mod` 后缀、放在 exe 旁的 `mods/` 目录、用的是 UTF-8 无 BOM，且 `#` 注释不要写在指令行首以外的位置，标点用英文半角。
- **进不去房间？** 检查主机和客户端是否在同一局域网、防火墙是否放行 UDP 25000 端口，IP 是否填写正确（主机 `ipconfig` / `ip addr` 查看）。
- **改完没变化？** 重启游戏；多个 mod 冲突时，靠后的文件会覆盖靠前的设置。

祝你修改愉快，钓遍共创的湖！